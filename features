Aim Assist
• Mode: Camlock / Mouse
• Keybind (default: C) -- can set to any keybind
• Activation: Toggle / Hold
• AimMode: ClosestPart / ClosestPoint / Specific Part
• Target Part (e.g. UpperTorso, Head, HumanoidRootPart)
• Smoothness control (0.01–1)
• EasingStyle (Linear, Quad, Cubic, Sine, etc.)
• Deadzone + Deadzone radius (pixels)

Silent Aim
• Mode: Target (requires lock) / FOV (independent)
• Closest Point prediction (Scalar / Generic)
• Closest Point scalar multiplier
• Anti-Curve (2D / 3D + angular limit in degrees)
• Client Bullet Redirection (weapon-specific)
• Prediction per weapon (e.g. 0.11–0.135)

Checks
• WallCheck
• UnlockOnWall (optional)
• StayOnTarget through walls
• Wall check throttle (anti-lag)
• Disable on reload
• Disable when knocked
• Skip ForceField players
• Skip friends

Range
• Range limit toggle
• Max distance (studs)

Triggerbot
• Trigger key (default: T) -- -- can set to any keybind
• Activation: Toggle / Hold
• Wall check
• Trigger delay (ms)
• Prediction value
• Blacklisted items (e.g. Knife, food.)

Resolver
• ResolverType: VelocityPredictor
• Manual prediction override (e.g. 0.1314)
• Max lead distance
• Velocity history size
• Prediction strength (0–1)
• Direction change sensitivity

Visuals
• FOV circle (color + transparency + thickness + filled + outline)
• Static or Dynamic FOV (min/max + distance scaling)
• Snapline/tracer (color + thickness + transparency + outline)

Movement
• Speed hack toggle
• WalkSpeed (16–500)
• JumpPower (50–300)
• Keybind (default: V) -- -- can set to any keybind

No Spread
• Spread amount (0 = none, 100 = default)

Rage – Orbit -- uses target bind,
• Mode: Strafe
• Distance from target (studs)
• Height offset (studs)
• Strafe speed (1–40)

Rage – BulletTP -- uses target bind
• Bullet teleport on shot
• Spectate target while active
• Target part (Head / UpperTorso / etc.)

Misc
• Anti-Sit
• No Jump Cooldown
• Anti-Void (prevent falling into void)
